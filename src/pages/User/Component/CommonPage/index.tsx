import { memo } from "react";
import { Form } from "~/components/Form";
import { Input } from "~/components/Input";
import { CommonPage } from "~/pages/CommonPage";

export const UserCommonPage = memo(() => {
    return (
        <div></div>
    )
});